package cx.ath.dekosuke.ftbt;

import java.io.IOException;
import java.io.StringReader;
import java.util.ArrayList;

import android.util.Log;
import android.util.Xml;

import java.util.regex.Pattern;
import java.util.regex.Matcher;

public class FutabaThreadRegexParser {

	// 本文パターンは全てこれで取れる
	// private static final textPattern =
	// Pattern.compile('<blockquote>(.+?)</blockquote>');
	// 画像パターン

	/*
	 * private String urlStr;
	 * 
	 * private ArrayList<FutabaStatus> statuses;
	 * 
	 * public FutabaThreadRegexParser(String urlStr) { this.urlStr = urlStr;
	 * title="(title)"; statuses = new ArrayList<FutabaStatus>(); }
	 * 
	 * // メモ:ふたばのスレッドはhtml-body-2つめのformのなかにある // スレッドの形式: public void parse() {
	 * XmlPullParser parser = Xml.newPullParser(); try { byte[] data =
	 * HttpClient.getByteArrayFromURL(urlStr); //parser.setInput(new
	 * StringReader(new String(data, "UTF-8"))); parser.setInput(new
	 * StringReader(new String(data, "Shift-JIS")));
	 * 
	 * } catch (Exception e) { Log.d( "ftbt", e.toString() ); throw new
	 * RuntimeException(e); } //return list; }
	 * 
	 * 
	 * public ArrayList<FutabaStatus> getStatuses(){ return statuses; }
	 */
}
